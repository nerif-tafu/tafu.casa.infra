api:
  insecure: true  # Enable this temporarily for debugging

entryPoints:
  web:
    address: ":80"

log:
  level: "DEBUG"  # Increase logging for debugging

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: bridge-network
    watch: true 