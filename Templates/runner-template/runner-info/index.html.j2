<!DOCTYPE html>
<html>
<head>
  <title>Runner Information</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 40px;
      line-height: 1.6;
    }
    h1 {
      color: #2c3e50;
    }
    .info {
      background-color: #f8f9fa;
      padding: 15px;
      border-radius: 4px;
      margin-top: 20px;
    }
    pre {
      background-color: #f5f5f5;
      padding: 10px;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <h1>Runner Information</h1>
  
  <div class="info">
    <p><strong>Runner:</strong> {{ runner | default('default') }}</p>
    <p><strong>Domain:</strong> {{ domain_full }}</p>
  </div>
  
  <h2>JSON Format</h2>
  <pre>
{
  "runner": "{{ runner | default('default') }}",
  "domain": "{{ domain_full }}"
}
  </pre>
</body>
</html> 